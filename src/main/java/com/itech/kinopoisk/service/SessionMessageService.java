package com.itech.kinopoisk.service;

import com.itech.kinopoisk.entity.SessionMessage;

public interface SessionMessageService {
    //add comment
    SessionMessage add(SessionMessage sessionMessage);
}
